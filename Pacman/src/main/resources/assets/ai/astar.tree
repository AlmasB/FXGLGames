#
# Pacman enemy 3 using A* pathfinding
# The path is updated only when agent reaches its previous target node
# so that player has some chance of survival
#

# Alias definitions

# Conditions

# Actions
import astarMove : "com.almasb.fxglgames.pacman.ai.AStarMoveAction"

# Tree definition (note that root is optional)
root
    astarMove
